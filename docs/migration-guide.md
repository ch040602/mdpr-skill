# Migration Guide

Keep the legacy MDPR pipeline as the default. Enable `design-components-rule-based` only through explicit CLI or config. Renderer entrypoints should accept both Layout IR and Styled Deck IR until migration is complete.
