# Component Showcase

Open `docs/component-showcase.html` to inspect the renderer-neutral component examples.

The page covers:

- Basic editable PPT shapes: rectangles, rounded rectangles, pills
- Z-order stacks: surface card, overlay, badge, text
- Chart/KPI surfaces
- Button/action states
- Tables
- Raster image frames
- Native PowerPoint chart objects
- Timeline/process rails
- Modal/sheet surfaces
- Motion fallback representations
- Mixed-object stress slides with a minimum readable text size

The examples are intentionally renderer-neutral. They describe how Design Components UI components map to editable PowerPoint shapes, HTML structure, and static PDF fallbacks.
