# Agent Hint Guide

Agent hints may suggest semantic intent, grouping, or importance. They cannot specify recipe IDs, variant IDs, boxes, coordinates, colors, or effects. Builds must remain valid with agents disabled.
